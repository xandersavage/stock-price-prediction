# Requirements for Stock Market Forecasting project
# Minimum recommended Python version
python>=3.8

# Data and numerical
pandas>=1.3
numpy>=1.21

# Visualization
matplotlib>=3.4
plotly>=5.0

# Time series / forecasting
statsmodels>=0.13
prophet>=1.0
cmdstanpy>=1.0  # optional — used by Prophet for the CmdStan backend

# Machine learning
xgboost>=1.6
scikit-learn>=1.0

# Notebook runtime (optional, if you work in Jupyter)
jupyterlab>=3.0

# Notes:
# - If you prefer the pystan backend for Prophet, replace or add 'pystan' instead of 'cmdstanpy'.
# - Version pins are intentionally loose; adjust versions to match your environment or CI as needed.
